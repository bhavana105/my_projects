<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog website</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1>Food Blogs website</h1>
        <a href="/articles/new" class="btn btn-primary">Create New Article</a>
        <div class="mt-4">
            <% articales.forEach(artical => { %>
                <div class="card mb-3" >
                    <div class="card-body">
                        <h5 class="card-title"><%=artical.title%></h5>
                        <p class="card-text"><small class="text-muted"><%= new Date(artical.createdAt).toLocaleDateString() %></small></p>
                        <p class="card-text"><%=artical.description%></p>
                        <div>
                            <a href="/articles/<%= artical.slug %>" class="btn btn-primary">Read More</a>
                            <a href="/articles/edit/<%= artical.id %>" class="btn btn-info">Edit</a>

                            <form action="/articles/<%= artical._id %>?_method=DELETE" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>

                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>

